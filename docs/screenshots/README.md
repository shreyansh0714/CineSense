# Deployment evidence screenshots

Drop the screenshots referenced in [`docs/DEPLOYMENT.md`](../DEPLOYMENT.md) here:

- `ecr-push.png` — successful `docker push` / image in ECR console
- `apprunner-running.png` — service in *Running* state with the public URL
- `apprunner-env.png` — `GEMINI_API_KEY` env var (masked) in service config
- `apprunner-autoscaling.png` — custom auto-scaling config, max size = 1
- `budget-alert.png` — $5/month budget with 80% email threshold
- `live-demo.png` — a full recommendation streaming on the public HTTPS URL
- `live-demo-mobile.png` — the same URL on a phone (responsive requirement)
